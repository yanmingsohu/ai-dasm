[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-dasm"
version = "0.1.0"

[tool.setuptools]
py-modules = ["dasm", "gen_rizin", "source", "utils"]